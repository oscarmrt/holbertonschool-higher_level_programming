#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new_list = ''
    for idx, x in enumerate(my_list):
        if x == search:
            new_list[idx] = replace
    return new_list
